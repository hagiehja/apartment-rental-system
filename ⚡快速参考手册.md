# ⚡ 快速参考手册（一页纸版本）

## 🎯 核心概念

```
资金守恒原则：
  支付前余额 = 支付后余额  ✅
  
  租客: 3000 → 0    (支付3000)
  房东: 0   → 3000  (收款3000)
  总额: 3000 → 3000 ✅
```

---

## 📁 文件一览

| 文件 | 功能 | 状态 |
|------|------|------|
| `stores/walletStore.js` | 全局钱包管理 | ✅ 新建 |
| `api/payment.js` | 支付API | ✅ 更新 |
| `views/contract/ContractDetail.vue` | 合同详情 | ✅ 更新 |
| `views/payment/Wallet.vue` | 钱包页面 | ✅ 更新 |
| `views/payment/Payment.vue` | 支付页面 | ⏳ 手动 |
| `main.js` | 应用入口 | ⏳ 手动 |

---

## 🔧 快速集成（5分钟）

### 1. 安装 Pinia
```bash
npm install pinia
```

### 2. 初始化 Pinia (main.js)
```javascript
import { createPinia } from 'pinia'
app.use(createPinia())
```

### 3. 复制文件
```bash
# 新建文件
cp stores/walletStore.js src/stores/

# 更新现有文件
cp api/payment.js src/api/
cp views/contract/ContractDetail.vue src/views/contract/
cp views/payment/Wallet.vue src/views/payment/
```

### 4. 手动更新 Payment.vue
- 第153行：添加导入 `useWalletStore`
- 第235行：替换 handlePay 函数

---

## 🚀 使用示例

### 在任何组件中刷新余额
```javascript
import { useWalletStore } from '@/stores/walletStore'

const walletStore = useWalletStore()
await walletStore.fullRefresh('payment')  // 刷新
console.log(walletStore.account.availableBalance)  // 查看余额
```

### 监听余额变化（跨页面）
```javascript
const unsubscribe = walletStore.onBalanceChange((reason, details) => {
  console.log('余额变化事件:', reason, details)
  // 更新页面显示
})

// 组件卸载时清理
onUnmounted(() => unsubscribe())
```

### 支付完整流程
```javascript
// 1. 验证资金
const valid = walletStore.validateFundsConservation(3000, 'payment')

// 2. 发起支付
await executePayment(paymentNo)

// 3. 立即刷新钱包
await walletStore.fullRefresh('payment')

// 4. 其他监听页面自动更新 ✅
```

---

## 🔐 关键步骤

```
提交支付/退款
  ↓
[前端验证] walletStore.validateFundsConservation()
  ↓
[发起API] executePayment() / executeAtomicRefund()
  ↓
[后端事务] @Transactional 保证原子性
  ↓
[等待500ms] 确保数据库提交
  ↓
[刷新状态] walletStore.fullRefresh()
  ↓
[触发事件] onBalanceChange 通知所有页面
  ↓
✅ 完成 - 所有页面自动更新
```

---

## 💾 Pinia Store API

```javascript
// 获取状态
walletStore.account.availableBalance

// 刷新余额（一个方法）
await walletStore.refreshBalance('manual')

// 完整刷新（推荐）
await walletStore.fullRefresh('payment')

// 验证资金守恒
walletStore.validateFundsConservation(amount, 'payment')

// 监听变化
walletStore.onBalanceChange(callback)

// 清空（退出登录）
walletStore.clearWallet()
```

---

## 📊 支付 API

```javascript
// 新增接口（在 payment.js 中）

// 1. 原子性退款
export function executeAtomicRefund(contractId, data) {
  return request({
    url: `/api/payment/contract/${contractId}/atomic-refund`,
    method: 'post',
    data
  })
}

// 2. 查询退款状态
export function getRefundStatus(contractId) {
  return request({
    url: `/api/payment/contract/${contractId}/refund-status`,
    method: 'get'
  })
}

// 3. 资金转移历史
export function getMoneyTransferHistory(startTime, endTime) {
  return request({
    url: '/api/payment/transfer-history',
    method: 'get',
    params: { startTime, endTime }
  })
}
```

---

## 🧪 测试流程

### 场景1：正常支付
```
1. 登录用户 (余额 3000)
2. 选择房源订单
3. 前往支付页面
4. 点击"确认支付"
5. ✅ 应该看到：余额变为 0，并立即刷新
6. ✅ 访问"我的钱包"页面：自动显示新余额
7. ✅ 交易记录中显示此次支付
```

### 场景2：申请退租
```
1. 导航到"我的合同"
2. 选择合同，点击"申请退款/退租"
3. 弹窗显示资金流向：房东 -¥500, 租客 +¥500
4. 输入退租原因，点击"确认退租"
5. ✅ 应该看到：您的余额增加 500
6. ✅ 钱包页面实时显示新余额
7. ✅ 显示绿色提示条"💰 退款已到账"
```

### 场景3：跨页面同步
```
1. 打开两个浏览器标签页
2. 标签A：支付 3000
3. 标签B（已打开钱包页面）：观察…
4. ✅ 标签B 应该自动显示新余额（不需手动刷新）
```

---

## 🐛 调试技巧

### 查看实时日志
```
F12 → Console 标签页 → 查看以下日志：
✅ 💰 支付前资金检查: {...}
✅ 💳 支付已完成，开始刷新余额...
✅ 🔄 钱包余额变化监听事件: payment {...}
✅ ✅ 支付成功，新余额: 2500.00
```

### 查看钱包状态
```javascript
// 在浏览器 Console 执行
import { useWalletStore } from '@/stores/walletStore'
const wallet = useWalletStore()
console.log(wallet.account)  // 查看余额详情
```

### 查看网络请求
```
F12 → Network 标签页 → 筛选 XHR
观察以下请求：
- GET /api/payment/account/balance
- GET /api/payment/account/transactions
- POST /api/payment/contract/{id}/atomic-refund
```

---

## ⚠️ 常见问题

| 问题 | 解决方案 |
|------|---------|
| 钱包页面不更新 | ✅ 检查是否调用了 `onBalanceChange()` |
| 支付后余额错误 | ✅ 检查 `/api/payment/account/balance` 响应 |
| Pinia 未定义 | ✅ 检查 `main.js` 中是否 `app.use(createPinia())` |
| 监听器多次触发 | ✅ 检查是否正确取消 (unsubscribe) |
| 实时通知条不显示 | ✅ 检查 `showRefreshTip` 的条件 |

---

## 📋 检查清单

```
□ npm install pinia
□ main.js 中 app.use(createPinia())
□ 复制 stores/walletStore.js
□ 更新 api/payment.js
□ 更新 views/contract/ContractDetail.vue
□ 更新 views/payment/Wallet.vue
□ 手动更新 views/payment/Payment.vue
□ npm run dev
□ 测试支付流程
□ 测试退租流程
□ 查看 Console 日志
□ 验证数据库事务配置
```

---

## 🎓 核心代码片段

### 支付完整流程
```javascript
// ContractDetail.vue 或 Payment.vue
const walletStore = useWalletStore()

try {
  // 1️⃣ 验证
  walletStore.validateFundsConservation(amount, 'payment')
  
  // 2️⃣ 支付
  await executePayment(paymentNo)
  
  // 3️⃣ 延迟
  await new Promise(r => setTimeout(r, 300))
  
  // 4️⃣ 刷新 🔑
  await walletStore.fullRefresh('payment')
  
  // 5️⃣ 完成 ✅
  alert('支付成功，新余额: ' + walletStore.account.availableBalance)
} catch(e) {
  alert('支付失败: ' + e.message)
}
```

### 钱包监听（一次性设置）
```javascript
// Wallet.vue setup()
const unsubscribe = walletStore.onBalanceChange((reason, details) => {
  // 显示实时通知
  showRefreshTip.value = true
  // 重新加载页面数据
  loadData()
})

onUnmounted(() => unsubscribe())
```

---

## 🌍 环境变量（可选）

```javascript
// 在需要时调整
const REFUND_DELAY = 300  // ms，等待后端事务
const NOTIFICATION_TTL = 3000  // ms，通知显示时长
const REFRESH_RETRY_TIMES = 3  // 刷新重试次数
```

---

**✅ 就这么简单！按照上述流程集成，您的前端已准备完毕！**

祝您使用愉快！🚀
