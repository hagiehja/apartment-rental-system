# 🔧 服务端口配置说明

## 📊 实际端口分配

| 服务 | 端口 | 访问路径 | 状态 |
|------|------|---------|------|
| **API网关** | **8080** | **所有API的统一入口** | ✅ |
| 用户服务 | 8081 | `/api/user/**` | ✅ |
| 房源服务 | 8083 | `/api/house/**` | ✅ |
| 订单服务 | 8088 | `/api/order/**` | ✅ |
| 支付服务 | 8087 | `/api/payment/**`, `/api/account/**` | ✅ |
| 通知服务 | 8091 | `/api/notification/**` | ✅ |
| 合同服务 | 8092 | `/api/contract/**` | ✅ |

## ⚠️ 重要提示

### Gateway配置已修正

之前Gateway配置中的端口错误已修复:
- ❌ 房源服务: 8082 → ✅ 8083
- ❌ 订单服务: 8083 → ✅ 8088
- ❌ 支付服务: 8084 → ✅ 8087

### 端口检查命令

Windows PowerShell:
```powershell
# 检查端口占用
netstat -ano | findstr "8080"  # Gateway
netstat -ano | findstr "8081"  # 用户服务
netstat -ano | findstr "8083"  # 房源服务
netstat -ano | findstr "8087"  # 支付服务
netstat -ano | findstr "8088"  # 订单服务
netstat -ano | findstr "8091"  # 通知服务
netstat -ano | findstr "8092"  # 合同服务
```

### 服务健康检查

通过Gateway访问:
```bash
# 用户服务
curl http://localhost:8080/api/user/health

# 房源服务
curl http://localhost:8080/api/house/health

# 订单服务
curl http://localhost:8080/api/order/health

# 支付服务
curl http://localhost:8080/api/payment/health

# 通知服务
curl http://localhost:8080/api/notification/health

# 合同服务
curl http://localhost:8080/api/contract/health
```

直接访问:
```bash
curl http://localhost:8081/api/user/health
curl http://localhost:8083/api/house/health
curl http://localhost:8088/api/order/health
curl http://localhost:8087/api/payment/health
curl http://localhost:8091/api/notification/health
curl http://localhost:8092/api/contract/health
```

## 📝 配置文件位置

- Gateway: `apartment-gateway/src/main/resources/application.yml`
- 用户服务: `apartment-user-service/src/main/resources/application.yml`
- 房源服务: `apartment-house-service/src/main/resources/application.yml`
- 订单服务: `apartment-order-service/src/main/resources/application.yml`
- 支付服务: `apartment-payment-service/src/main/resources/application.yml`
- 通知服务: `apartment-notice-service/src/main/resources/application.yml`
- 合同服务: `apartment-contract-service/src/main/resources/application.yml`

---

**更新时间**: 2026-01-27 23:32  
**状态**: Gateway端口配置已修复
