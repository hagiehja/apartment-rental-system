# 📦 前端核心文件清单与集成指南

**本文档汇总了所有需要修改/创建的前端文件**

---

## 📂 文件列表

### ✅ 已完成的文件

#### 1️⃣ **状态管理** - `stores/walletStore.js`（新建）
- **功能**: 全局钱包状态管理、实时余额刷新、跨页面监听
- **关键方法**:
  - `loadWalletData()` - 初始化加载
  - `refreshBalance(reason)` - 实时刷新余额
  - `fullRefresh(reason)` - 完整刷新（余额+交易）
  - `onBalanceChange(callback)` - 注册变化监听
  - `validateFundsConservation(amount, type)` - 资金守恒验证
- **使用场景**: 所有需要钱包数据的组件
- **状态**: ✅ 完成

#### 2️⃣ **支付 API** - `api/payment.js`（已更新）
- **新增方法**:
  ```javascript
  executeAtomicRefund(contractId, data)     // 原子性退款
  getRefundStatus(contractId)               // 获取退款状态
  getMoneyTransferHistory(startTime, endTime)  // 查询转移历史
  ```
- **状态**: ✅ 完成

#### 3️⃣ **合同详情** - `views/contract/ContractDetail.vue`（已更新）
- **更新内容**:
  - 导入 `useWalletStore`
  - `submitTerminate()` 增强：原子性操作 + 实时刷新
  - 增加资金守恒可视化展示
  - 增加详细的操作日志 (console.log)
- **关键步骤**:
  1. 前端验证资金守恒
  2. 显示资金流向确认
  3. 调用后端API
  4. **延迟500ms等待事务**
  5. 调用 `walletStore.fullRefresh('refund')`
  6. 显示新余额
- **状态**: ✅ 完成

#### 4️⃣ **钱包页面** - `views/payment/Wallet.vue`（已更新）
- **更新内容**:
  - 导入 `useWalletStore`
  - 增加实时通知条 `.refresh-notification`
  - 增加手动刷新按钮
  - 设置余额变化监听器
  - 监听页面加载和卸载生命周期
- **功能**:
  - 自动监听其他页面的操作
  - 实时显示余额变化动画
  - 支持手动刷新
- **状态**: ✅ 完成

#### 5️⃣ **支付页面** - `views/payment/Payment.vue`（需更新）
- **更新内容**:
  - 导入 `useWalletStore`
  - `handlePay()` 方法增强：资金守恒验证 + 实时刷新
  - 支付后调用 `walletStore.fullRefresh('payment')`
  - 显示新余额信息
- **关键代码**: 见本文档下方 🔽
- **状态**: ⏳ **需要手动更新**（见下方）

---

## 🔧 需要手动更新的文件

### Payment.vue 更新指南

**文件位置**: `frontend/src/views/payment/Payment.vue`

**步骤 1**: 在 script 顶部添加导入

```javascript
// 第 153 行附近，修改为：
import { ref, computed, onMounted, onUnmounted } from 'vue'
import { useRouter, useRoute } from 'vue-router'
import { getOrderDetail } from '../../api/order'
import { getAccountBalance, createPayment, executePayment } from '../../api/payment'
import { useWalletStore } from '../../stores/walletStore'  // ✅ 新增

export default {
  name: 'Payment',
  setup() {
    const router = useRouter()
    const route = useRoute()
    const walletStore = useWalletStore()  // ✅ 新增
```

**步骤 2**: 替换 `handlePay` 函数

找到 line ~235 的 `// 确认支付` 注释，替换整个函数为：

```javascript
    // 确认支付（原子性操作 - 资金守恒）
    const handlePay = async () => {
      if (balance.value < payAmount.value) {
        alert('余额不足，请先充值')
        return
      }
      
      paying.value = true
      try {
        // 1. 验证资金守恒
        const validation = walletStore.validateFundsConservation(
          parseFloat(payAmount.value),
          'payment'
        )
        console.log('💰 支付前资金检查:', validation)
        
        // 2. 创建支付单
        const createRes = await createPayment({
          orderNo: order.value.orderNo,
          amount: payAmount.value,
          paymentMethod: 'BALANCE'
        })
        
        // 3. 执行支付（原子性操作）
        await executePayment(createRes.data.paymentNo)
        
        // 4. 关键步骤：支付成功后，立即刷新钱包（保证实时性）
        console.log('💳 支付已完成，开始刷新余额...')
        
        // 短暂延迟，确保后端事务提交
        await new Promise(resolve => setTimeout(resolve, 300))
        
        // 通过钱包Store刷新，会自动触发全站监听
        const refreshSuccess = await walletStore.fullRefresh('payment')
        
        if (refreshSuccess) {
          console.log('✅ 支付成功，新余额:', walletStore.account.availableBalance)
          
          alert(
            `✅ 支付成功！\n\n` +
            `支付金额：¥${payAmount.value.toFixed(2)}\n` +
            `✨ 您的新余额：¥${walletStore.account.availableBalance.toFixed(2)}\n\n` +
            `租赁合同已自动生成。`
          )
        } else {
          // 前端显示虽然支付成功了，但余额刷新失败
          balance.value = walletStore.account.availableBalance
          
          alert(
            `支付成功，但实时余额加载失败。\n` +
            `请访问"我的钱包"查看最新余额。`
          )
        }
        
        router.push(`/order/${order.value.orderNo}`)
      } catch (error) {
        console.error('❌ 支付失败:', error)
        alert(error.message || '支付失败，请重试')
      } finally {
        paying.value = false
      }
    }
```

---

## 🗂️ 文件结构总览

```
frontend/
├── src/
│   ├── stores/
│   │   └── walletStore.js                      ✅ 新建
│   ├── api/
│   │   └── payment.js                          ✅ 已更新
│   ├── views/
│   │   ├── contract/
│   │   │   ├── ContractDetail.vue              ✅ 已更新
│   │   │   └── ContractList.vue                (无需改)
│   │   ├── payment/
│   │   │   ├── Payment.vue                     ⏳ 需手动更新
│   │   │   ├── Wallet.vue                      ✅ 已更新
│   │   │   └── ...
│   │   └── ...
│   └── main.js                                 ⏳ 需初始化 Pinia
└── package.json                                ⏳ 需添加依赖
```

---

## 📦 依赖安装

### Step 1: 安装 Pinia

```bash
cd frontend
npm install pinia
```

### Step 2: 在 main.js 中初始化

```javascript
// frontend/src/main.js
import { createApp } from 'vue'
import { createPinia } from 'pinia'  // ✅ 新增
import App from './App.vue'
import router from './router'

const app = createApp(App)

app.use(createPinia())  // ✅ 新增
app.use(router)
app.mount('#app')
```

### Step 3: 验证安装

```bash
npm list | grep pinia
# 应该看到 pinia 的版本信息
```

---

## 🧪 集成验证

### 检查清单

- [ ] `stores/walletStore.js` 已创建
- [ ] `api/payment.js` 已更新新接口
- [ ] `views/contract/ContractDetail.vue` 已导入 useWalletStore
- [ ] `views/contract/ContractDetail.vue` submitTerminate() 已增强
- [ ] `views/payment/Wallet.vue` 已更新实时监听
- [ ] `views/payment/Payment.vue` 已更新 handlePay()
- [ ] `main.js` 已初始化 Pinia
- [ ] `package.json` 已添加 pinia 依赖

### 测试流程

```
1. npm install pinia
2. npm run dev

3. 打开 http://localhost:5173
4. 登录用户
5. 导航到"订单支付"页面
6. 执行支付 → 检查控制台日志 → 查看"我的钱包"自动刷新
7. 导航到"我的合同" → 申请退租 → 检查余额变化
8. F12 打开控制台，查看所有 console.log 输出
```

---

## 🔍 调试技巧

### 1. 查看 Pinia 状态

```javascript
// 在任何组件中
import { useWalletStore } from '@/stores/walletStore'
const walletStore = useWalletStore()

// 浏览器控制台执行
window.__PINIA__ = pinia  // 暴露 Pinia 到全局
```

### 2. 查看完整日志

按 **F12** 打开开发者工具，在 Console 标签页查看：

```
💰 支付前资金检查: {...}
💳 支付已完成，开始刷新余额...
🔄 开始刷新钱包余额...
✅ 支付成功，新余额: 2500.00
💳 钱包余额变化监听事件: payment {...}
```

### 3. 网络请求追踪

在 Network 标签页查看 API 请求：
- `GET /api/payment/account/balance` - 余额查询
- `GET /api/payment/account/transactions` - 交易记录
- `POST /api/payment/contract/{id}/atomic-refund` - 退款请求

---

## 📝 关键概念速记

| 概念 | 解释 | 示例 |
|------|------|------|
| **原子性** | 操作要么全部成功，要么全部失败 | 支付时租客-X，房东+X 必须同时成功 |
| **资金守恒** | 两个账户的总额前后不变 | 支付前：3000, 支付后：3000 ✅ |
| **实时刷新** | 操作后立即同步前端状态 | 支付完成 → 自动显示新余额 |
| **事件监听** | 其他页面自动响应数据变化 | 支付页完成 → 钱包页自动更新 |
| **原子性操作** | 数据库事务层面的保证 | `@Transactional` 注解 |

---

## 🚀 部署前检查

- [ ] 所有文件已保存
- [ ] `npm run build` 无错误
- [ ] 后端接口已实现
- [ ] 数据库事务配置已设置
- [ ] 测试场景都能通过
- [ ] 控制台无红色错误

---

## 📚 完整代码文件

所有代码文件已按以下结构生成：

```
项目根目录/
├── stores/walletStore.js                    (新文件，已生成)
├── api/payment.js                           (已更新)
├── views/contract/ContractDetail.vue        (已更新)
├── views/payment/Wallet.vue                 (已更新)
├── views/payment/Payment.vue                (需手动更新)
├── 前端实现指南-合同取消与退款.md           (详细文档)
├── 后端接口规范-原子性退款.md               (后端参考)
└── fix-payment.js                           (自动更新脚本)
```

---

## 💬 常见问题

### Q1: 钱包页面没有实时更新？
**A**: 检查：
1. 是否导入了 `useWalletStore`
2. 是否调用了 `setupBalanceListener()`
3. 是否在 `onMounted` 中初始化

### Q2: 支付后看不到新余额？
**A**: 检查：
1. `walletStore.fullRefresh('payment')` 是否返回 true
2. 后端接口 `/api/payment/account/balance` 是否返回正确数据
3. Network 标签页中请求是否 200 OK

### Q3: 多个浏览器标签页如何同步？
**A**: 当前实现是独立的，刷新后会同步最新数据。如需自动同步，可使用 localStorage 事件。

### Q4: 如何禁用自动刷新提示？
**A**: 在 Wallet.vue 中找到 `setTimeout(() => { showRefreshTip.value = false }, 3000)` 改为较大的值或移除。

---

## 🎓 学习资源

- **Pinia 官方文档**: https://pinia.vuejs.org/
- **Vue 3 Composition API**: https://vuejs.org/guide/extras/composition-api-faq.html
- **前端状态管理**: https://github.com/vuejs/awesome-vue

---

**🎉 恭喜！您的前端实现已准备完毕！**

下一步：
1. ✅ 复制所有生成的文件
2. ✅ 手动更新 Payment.vue
3. ✅ 后端团队实现接口规范
4. ✅ 运行集成测试
5. ✅ 部署上线

祝您开发顺利！🚀
