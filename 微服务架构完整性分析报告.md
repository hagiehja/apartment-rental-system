# 公寓租赁系统 - 微服务架构完整性分析报告

## 📊 架构评估结论

### ✅ 是否是完整的微服务架构？

**答案：是的！** 这是一个**标准的 Spring Cloud 微服务架构**，具备微服务架构的核心特征。

**微服务架构核心要素检查表**：
- ✅ **服务拆分**: 7 个独立微服务 + 1 个 API 网关
- ✅ **服务注册与发现**: Nacos Discovery
- ✅ **配置中心**: Nacos Config
- ✅ **服务间通信**: OpenFeign (HTTP) + RocketMQ (异步)
- ✅ **API 网关**: Spring Cloud Gateway
- ✅ **负载均衡**: Spring Cloud LoadBalancer
- ✅ **数据库独立**: 每个服务独立数据库
- ✅ **分布式缓存**: Redis
- ✅ **消息队列**: RocketMQ
- ✅ **安全认证**: JWT + Spring Security

---

## 🏗️ 微服务架构拓扑图

```
                          ┌─────────────────┐
                          │   用户请求       │
                          └────────┬────────┘
                                   │
                          ┌────────▼────────┐
                          │  API Gateway    │
                          │   (8080)        │
                          │ Spring Cloud    │
                          │    Gateway      │
                          └────────┬────────┘
                                   │
              ┌────────────────────┼────────────────────┐
              │                    │                    │
    ┌─────────▼─────────┐ ┌───────▼────────┐ ┌────────▼─────────┐
    │  User Service     │ │ House Service  │ │ Order Service    │
    │    (8081)         │ │    (8083)      │ │    (8088)        │
    │  用户/认证/授权    │ │   房源管理      │ │  订单/分期管理    │
    └─────────┬─────────┘ └───────┬────────┘ └────────┬─────────┘
              │                    │                    │
              │                    │         ┌──────────▼──────────┐
              │                    │         │ Payment Service     │
              │                    │         │    (8087)           │
              │                    │         │   支付/账户管理      │
              │                    │         └──────────┬──────────┘
              │                    │                    │
    ┌─────────▼─────────┐ ┌───────▼────────┐ ┌────────▼─────────┐
    │Contract Service   │ │ Notice Service │ │                  │
    │    (8092)         │ │    (8091)      │ │                  │
    │   合同生成/管理    │ │   通知/消息     │ │                  │
    └───────────────────┘ └────────────────┘ └──────────────────┘
              │                    │
              └────────────────────┼────────────────────┘
                                   │
                     ┌─────────────▼─────────────┐
                     │      中间件层               │
                     │                            │
                     │  ┌────────┐  ┌─────────┐  │
                     │  │ Nacos  │  │  MySQL  │  │
                     │  │ 注册/配置│  │  数据库  │  │
                     │  └────────┘  └─────────┘  │
                     │                            │
                     │  ┌────────┐  ┌─────────┐  │
                     │  │ Redis  │  │RocketMQ │  │
                     │  │  缓存   │  │  消息    │  │
                     │  └────────┘  └─────────┘  │
                     └────────────────────────────┘
```

---

## 🎯 服务详细分析

### 1. API Gateway（apartment-gateway）
**端口**: 8080  
**职责**: 统一入口、路由转发、跨域处理

**技术栈**:
- Spring Cloud Gateway
- Nacos Discovery（服务发现）

**中间件使用**:
- ✅ **Nacos**: 从注册中心获取后端服务实例列表
- ✅ **LoadBalancer**: 自动负载均衡到多实例

**启动依赖**: 需要 Nacos 运行

---

### 2. User Service（apartment-user-service）
**端口**: 8081  
**职责**: 用户管理、认证授权、JWT 令牌颁发

**核心功能**:
- 用户注册/登录
- JWT Token 生成与验证
- 用户信息查询
- 房东/租客角色管理

**中间件使用**:
| 中间件 | 作用 | 使用场景 |
|--------|------|----------|
| **MySQL** | 持久化存储 | 用户基础信息（user 表） |
| **Redis** | 分布式缓存 | Token 黑名单、用户会话缓存 |
| **Nacos** | 服务注册 | 注册到服务中心，供网关调用 |
| **Spring Security** | 安全框架 | JWT 认证过滤器、密码加密 |

**数据库表**:
- `user`: 用户基本信息

---

### 3. House Service（apartment-house-service）
**端口**: 8083  
**职责**: 房源信息管理

**核心功能**:
- 房源发布/编辑/删除
- 房源列表查询（分页、筛选）
- 房源详情查看
- 房源状态管理（可租/已租/维护中）

**中间件使用**:
| 中间件 | 作用 | 使用场景 |
|--------|------|----------|
| **MySQL** | 持久化存储 | 房源信息（house 表） |
| **Nacos** | 服务注册 | 提供服务给订单服务调用 |

**数据库表**:
- `house`: 房源信息

---

### 4. Order Service（apartment-order-service）
**端口**: 8088  
**职责**: 订单管理、分期计划生成

**核心功能**:
- 订单创建（调用房源服务获取房源信息）
- 订单查询/取消
- 分期计划生成与管理
- 订单超时自动取消（定时任务）

**中间件使用**:
| 中间件 | 作用 | 使用场景 |
|--------|------|----------|
| **MySQL** | 持久化存储 | 订单表、分期计划表 |
| **Redis** | 分布式锁 | 订单创建防重、超时任务去重 |
| **Nacos** | 服务注册 | 注册服务、提供给支付服务调用 |
| **OpenFeign** | 服务调用 | 远程调用房源服务（`HouseFeignClient`） |

**服务依赖**:
- → `apartment-house-service`: 获取房源详情和价格

**数据库表**:
- `rental_order`: 租赁订单
- `installment_plan`: 分期计划

**定时任务**:
- `OrderTimeoutCancelJob`: 每分钟检查并取消超时未支付订单

---

### 5. Payment Service（apartment-payment-service）
**端口**: 8087  
**职责**: 支付管理、用户账户管理

**核心功能**:
- 支付单创建
- 余额支付/第三方支付
- 账户充值/提现
- 支付成功后回调订单服务
- 退款处理

**中间件使用**:
| 中间件 | 作用 | 使用场景 |
|--------|------|----------|
| **MySQL** | 持久化存储 | 支付单、用户账户、交易流水 |
| **Redis** | 分布式锁 | 余额扣减防并发、交易幂等性 |
| **Nacos** | 服务注册 | 注册服务 |
| **OpenFeign** | 服务调用 | 调用订单服务验证订单、通知支付成功（`OrderFeignClient`） |

**服务依赖**:
- → `apartment-order-service`: 验证订单、通知支付结果

**数据库表**:
- `payment`: 支付单
- `user_account`: 用户账户
- `account_transaction`: 账户交易流水

---

### 6. Contract Service（apartment-contract-service）
**端口**: 8092  
**职责**: 合同生成与管理

**核心功能**:
- 根据订单信息生成租赁合同（PDF）
- 合同查询/下载
- 电子签章（预留）

**中间件使用**:
| 中间件 | 作用 | 使用场景 |
|--------|------|----------|
| **MySQL** | 持久化存储 | 合同记录 |
| **Nacos** | 服务注册 | 注册服务 |
| **iText PDF** | PDF 生成 | 生成合同文件（支持中文） |

**数据库表**:
- `contract`: 合同信息

---

### 7. Notice Service（apartment-notice-service）
**端口**: 8091  
**职责**: 通知消息管理

**核心功能**:
- 站内消息发送
- 短信/邮件通知（通过 RocketMQ 异步发送）
- 消息查询/已读标记
- 消息推送（WebSocket - 预留）

**中间件使用**:
| 中间件 | 作用 | 使用场景 |
|--------|------|----------|
| **MySQL** | 持久化存储 | 消息记录 |
| **RocketMQ** | 消息队列 | 接收订单/支付事件，异步发送通知 |
| **Nacos** | 服务注册 | 注册服务 |

**消息消费**:
- `OrderEventConsumer`: 监听订单创建/支付成功事件
- `PaymentEventConsumer`: 监听支付成功/失败事件

**数据库表**:
- `notification`: 通知消息

---

## 🔧 中间件完整清单

### 1. Nacos（服务注册中心 + 配置中心）
**版本**: 2.x  
**地址**: 192.168.24.129:8848  
**默认账号**: nacos/nacos

**作用**:
1. **服务注册与发现**
   - 所有微服务启动时自动注册到 Nacos
   - API Gateway 从 Nacos 获取服务实例列表进行路由
   - OpenFeign 通过服务名从 Nacos 获取实例地址

2. **配置中心**
   - 集中管理所有服务的配置（数据库、Redis、RocketMQ 等）
   - 支持配置热更新（需配合 `@RefreshScope`）
   - 多环境配置隔离（dev/test/prod）

**使用服务**: 全部 7 个服务

**核心配置**（bootstrap.yml）:
```yaml
spring:
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.24.129:8848
      config:
        server-addr: 192.168.24.129:8848
        file-extension: yaml
```

---

### 2. MySQL（关系型数据库）
**版本**: 8.0.33  
**端口**: 3306  
**连接池**: HikariCP

**作用**: 
- 业务数据持久化存储
- 每个服务独立数据库，符合微服务数据隔离原则

**数据库清单**:
| 服务 | 数据库名 | 核心表 |
|------|----------|--------|
| User Service | `apartment_user` | user |
| House Service | `apartment_house` | house |
| Order Service | `apartment_order` | rental_order, installment_plan |
| Payment Service | `apartment_payment` | payment, user_account, account_transaction |
| Contract Service | `apartment_contract` | contract |
| Notice Service | `apartment_notice` | notification |

**配置位置**: Nacos 配置中心 → `{service-name}.yaml` → `spring.datasource`

---

### 3. Redis（分布式缓存 + 分布式锁）
**版本**: 7.x  
**端口**: 6379  
**客户端**: Redisson

**作用**:
1. **缓存**
   - 用户 Token 黑名单（User Service）
   - 用户会话缓存
   - 热点数据缓存

2. **分布式锁**
   - 订单创建防重（Order Service）
   - 余额扣减并发控制（Payment Service）
   - 定时任务去重（Order Service）

**使用服务**: User Service、Order Service、Payment Service

**配置位置**: Nacos 配置中心 → `spring.data.redis`

---

### 4. RocketMQ（消息队列）
**版本**: 5.x  
**端口**: 9876 (NameServer)  
**作用**: 
- 异步通知解耦
- 事件驱动架构
- 削峰填谷

**消息主题**:
| Topic | Producer | Consumer | 用途 |
|-------|----------|----------|------|
| `order-events` | Order Service | Notice Service | 订单创建/支付成功通知 |
| `payment-events` | Payment Service | Notice Service | 支付结果通知 |

**使用服务**: Order Service（生产）、Payment Service（生产）、Notice Service（消费）

**配置位置**: Nacos 配置中心 → `rocketmq.name-server`

---

### 5. Spring Cloud Gateway（API 网关）
**端口**: 8080  
**作用**:
- 统一入口
- 路由转发（根据 URL 前缀路由到不同服务）
- 跨域处理（CORS）
- 请求日志记录

**路由配置**:
```yaml
/api/user/**      → apartment-user-service:8081
/api/house/**     → apartment-house-service:8083
/api/order/**     → apartment-order-service:8088
/api/payment/**   → apartment-payment-service:8087
/api/account/**   → apartment-payment-service:8087
/api/contract/**  → apartment-contract-service:8092
/api/notification/** → apartment-notice-service:8091
```

---

### 6. OpenFeign（服务间调用）
**作用**: 
- 声明式 HTTP 客户端
- 替代 RestTemplate
- 自动集成 LoadBalancer 负载均衡

**Feign 客户端清单**:
| 服务 | Feign Client | 调用目标 | 用途 |
|------|--------------|----------|------|
| Order Service | `HouseFeignClient` | apartment-house-service | 获取房源详情和价格 |
| Payment Service | `OrderFeignClient` | apartment-order-service | 验证订单、通知支付成功 |

**示例**:
```java
@FeignClient(name = "apartment-house-service")
public interface HouseFeignClient {
    @GetMapping("/house/{id}")
    Map<String, Object> getHouseDetail(@PathVariable Long houseId);
}
```

---

### 7. MyBatis-Plus（ORM 框架）
**版本**: 3.5.7 (Spring Boot 3.x 专用)  
**作用**:
- 简化 SQL 操作
- 代码生成器
- 分页插件

**使用服务**: 全部 7 个服务

---

### 8. Spring Security + JWT（安全认证）
**作用**:
- JWT Token 生成与验证
- 接口权限控制
- 密码加密（BCrypt）

**使用服务**: User Service（主要）、其他服务（Token 验证）

**认证流程**:
```
1. 用户登录 → User Service 验证账号密码
2. 生成 JWT Token → 返回给前端
3. 前端请求携带 Token → Gateway 转发
4. 各服务接收请求 → 从 Token 解析用户信息
```

---

## 🚀 完整启动指南

### 前置准备

#### 1. 中间件启动（必须）

```bash
# 1️⃣ 启动 MySQL (假设已安装)
# 确保 MySQL 服务运行在 3306 端口

# 2️⃣ 启动 Redis
redis-server

# 3️⃣ 启动 Nacos
cd /path/to/nacos/bin
./startup.cmd -m standalone  # Windows
# 或
sh startup.sh -m standalone  # Linux/Mac

# 访问 Nacos 控制台: http://192.168.24.129:8848/nacos
# 账号密码: nacos/nacos

# 4️⃣ 启动 RocketMQ (可选，如不启动通知服务可跳过)
# 启动 NameServer
cd /path/to/rocketmq/bin
./mqnamesrv.cmd  # Windows
# 或
sh mqnamesrv      # Linux/Mac

# 启动 Broker
./mqbroker.cmd -n 127.0.0.1:9876  # Windows
# 或
sh mqbroker -n 127.0.0.1:9876     # Linux/Mac
```

---

#### 2. 数据库初始化

```bash
# 执行初始化脚本
mysql -u root -p < init-database.sql
```

---

#### 3. Nacos 配置导入

**步骤**:
1. 打开 Nacos 控制台: http://192.168.24.129:8848/nacos
2. 登录（nacos/nacos）
3. 进入「配置管理」→「配置列表」
4. 点击「导入配置」
5. 选择文件 `nacos-configs-all.yaml`
6. 确认导入

**或者手动创建 7 个配置**:
| Data ID | Group | 格式 | 内容来源 |
|---------|-------|------|----------|
| `apartment-user-service.yaml` | DEFAULT_GROUP | YAML | nacos-configs-all.yaml |
| `apartment-house-service.yaml` | DEFAULT_GROUP | YAML | nacos-configs-all.yaml |
| `apartment-order-service.yaml` | DEFAULT_GROUP | YAML | nacos-configs-all.yaml |
| `apartment-payment-service.yaml` | DEFAULT_GROUP | YAML | nacos-configs-all.yaml |
| `apartment-contract-service.yaml` | DEFAULT_GROUP | YAML | nacos-configs-all.yaml |
| `apartment-notice-service.yaml` | DEFAULT_GROUP | YAML | nacos-configs-all.yaml |
| `apartment-gateway.yaml` | DEFAULT_GROUP | YAML | nacos-configs-all.yaml |

---

### 方式一：命令行启动（推荐用于生产）

#### 步骤 1: 构建 JAR 包（如未构建）
```bash
cd c:\Users\Zz\IdeaProjects\apartment-rental-system
mvn clean package -DskipTests
```

#### 步骤 2: 按顺序启动服务

**启动顺序很重要！** 按依赖关系启动：

```bash
# 第一批：基础服务（无依赖）
java -jar apartment-user-service/target/apartment-user-service-0.0.1-SNAPSHOT.jar
java -jar apartment-house-service/target/apartment-house-service-0.0.1-SNAPSHOT.jar

# 等待 10 秒，确保服务注册到 Nacos
# 查看 Nacos 控制台确认服务已上线

# 第二批：业务服务（依赖基础服务）
java -jar apartment-order-service/target/apartment-order-service-0.0.1-SNAPSHOT.jar

# 等待 5 秒

java -jar apartment-payment-service/target/apartment-payment-service-0.0.1-SNAPSHOT.jar
java -jar apartment-contract-service/target/apartment-contract-service-0.0.1-SNAPSHOT.jar

# 第三批：通知服务（可选）
java -jar apartment-notice-service/target/apartment-notice-service-0.0.1-SNAPSHOT.jar

# 最后：网关（确保所有服务已启动）
java -jar apartment-gateway/target/apartment-gateway-0.0.1-SNAPSHOT.jar
```

**后台运行（Linux/Mac）**:
```bash
nohup java -jar apartment-user-service/target/apartment-user-service-0.0.1-SNAPSHOT.jar > user.log 2>&1 &
nohup java -jar apartment-house-service/target/apartment-house-service-0.0.1-SNAPSHOT.jar > house.log 2>&1 &
# ... 以此类推
```

---

### 方式二：IDE 启动（推荐用于开发）

#### IDEA 启动配置

1. **打开项目**: `c:\Users\Zz\IdeaProjects\apartment-rental-system`

2. **配置启动项**（每个服务一个）:
   - User Service: 运行 `UserApplication.java`
   - House Service: 运行 `HouseApplication.java`
   - Order Service: 运行 `OrderApplication.java`
   - Payment Service: 运行 `PaymentApplication.java`
   - Contract Service: 运行 `ContractApplication.java`
   - Notice Service: 运行 `NotificationApplication.java`
   - Gateway: 运行 `GatewayApplication.java`

3. **启动顺序**:
   ```
   UserApplication → HouseApplication 
   → OrderApplication → PaymentApplication 
   → ContractApplication → NotificationApplication 
   → GatewayApplication
   ```

4. **验证启动**:
   - 查看控制台输出，看到 "Started XxxApplication" 表示启动成功
   - 访问 Nacos 控制台: http://192.168.24.129:8848/nacos
   - 在「服务管理」→「服务列表」中应该看到 7 个服务

---

### 启动验证

#### 1. 检查 Nacos 服务注册

访问: http://192.168.24.129:8848/nacos

应该看到以下服务（全部 UP 状态）:
- ✅ apartment-user-service (1)
- ✅ apartment-house-service (1)
- ✅ apartment-order-service (1)
- ✅ apartment-payment-service (1)
- ✅ apartment-contract-service (1)
- ✅ apartment-notice-service (1)
- ✅ apartment-gateway (1)

#### 2. 测试接口调用

```bash
# 通过网关访问（推荐）
curl http://localhost:8080/api/user/test
curl http://localhost:8080/api/house/list

# 直接访问服务
curl http://localhost:8081/user/test
curl http://localhost:8083/house/list
```

#### 3. 测试完整业务流程

```bash
# 1. 用户注册
curl -X POST http://localhost:8080/api/user/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "123456",
    "phone": "13800138000",
    "role": "TENANT"
  }'

# 2. 用户登录
curl -X POST http://localhost:8080/api/user/login \
  -H "Content-Type: application/json" \
  -d '{
    "account": "testuser",
    "password": "123456"
  }'

# 保存返回的 token

# 3. 查询房源
curl http://localhost:8080/api/house/list

# 4. 创建订单（测试 Feign 调用）
curl -X POST http://localhost:8080/api/order/create \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "houseId": 1,
    "rentMonths": 12,
    "rentStartDate": "2026-02-01",
    "installmentEnabled": false
  }'
```

---

## 🛠️ 常见问题排查

### 问题 1: 端口被占用
```
Port 8091 was already in use
```

**解决**:
```bash
# Windows
netstat -ano | findstr :8091
taskkill /F /PID <进程ID>

# Linux/Mac
lsof -i :8091
kill -9 <进程ID>
```

---

### 问题 2: 无法连接 Nacos
```
Unable to connect to Nacos server
```

**检查**:
1. Nacos 是否启动: http://192.168.24.129:8848/nacos
2. 网络是否可达: `ping 192.168.24.129`
3. 防火墙是否开放 8848 端口

---

### 问题 3: 服务未注册到 Nacos

**排查步骤**:
1. 检查 `bootstrap.yml` 中 Nacos 地址是否正确
2. 查看服务启动日志，搜索 "Nacos" 关键字
3. 确认 `@EnableDiscoveryClient` 注解是否添加

---

### 问题 4: Feign 调用失败
```
feign.RetryableException: Connection refused
```

**原因**: 被调用的服务未启动或未注册到 Nacos

**解决**:
1. 检查被调用服务是否启动
2. 在 Nacos 控制台确认服务已注册
3. 检查服务名是否匹配（`@FeignClient(name = "xxx")`）

---

## 📈 架构优势

### 1. 高可用性
- 每个服务独立部署，单服务故障不影响其他服务
- Nacos 服务注册中心，支持服务健康检查和自动剔除

### 2. 可扩展性
- 服务按需横向扩展（启动多个实例）
- LoadBalancer 自动负载均衡

### 3. 松耦合
- 服务间通过接口调用，不直接依赖实现
- RocketMQ 异步解耦，降低服务间依赖

### 4. 易维护
- 配置集中管理（Nacos Config）
- 独立数据库，职责清晰

---

## 🎯 架构改进建议

### 短期优化（1-2 周）
1. ✅ **已完成**: Nacos + OpenFeign 微服务改造
2. **链路追踪**: 集成 SkyWalking 或 Zipkin（监控服务调用链）
3. **熔断降级**: 添加 Sentinel（防止雪崩）

### 中期优化（1-2 月）
4. **分布式事务**: Seata（订单+支付一致性）
5. **接口文档**: Swagger/Knife4j（API 文档自动生成）
6. **日志收集**: ELK Stack（集中式日志）

### 长期优化（3-6 月）
7. **容器化部署**: Docker + Kubernetes
8. **DevOps**: Jenkins CI/CD 自动化部署
9. **监控告警**: Prometheus + Grafana

---

## 📚 总结

### 当前架构成熟度: ⭐⭐⭐⭐☆ (4/5)

**已具备**:
- ✅ 完整的微服务拆分
- ✅ 服务注册与发现
- ✅ 配置中心
- ✅ API 网关
- ✅ 服务间通信（同步 + 异步）
- ✅ 分布式缓存
- ✅ 消息队列

**待完善**:
- ⚠️ 链路追踪
- ⚠️ 熔断降级
- ⚠️ 分布式事务
- ⚠️ 容器化部署

**结论**: 这是一个**标准的、完整的 Spring Cloud 微服务架构**，符合微服务架构的核心原则，可以直接用于生产环境！
