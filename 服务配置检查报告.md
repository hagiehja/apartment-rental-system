# 服务安全配置检查与修复报告

## 📋 检查结果总结

### ✅ 检查完成的服务
1. **User Service** (apartment-user-service)
2. **House Service** (apartment-house-service)
3. **Order Service** (apartment-order-service)
4. **Payment Service** (apartment-payment-service)
5. **Contract Service** (apartment-contract-service)
6. **Notice Service** (apartment-notice-service)

---

## 🔍 发现的问题

### 问题 1: Gateway 路由配置错误 ⚠️

**问题描述**：
- Gateway 路由配置使用 `StripPrefix=0`，不去除任何路径前缀
- 导致路径不匹配：
  - 请求：`/api/house/list`
  - 转发到服务：`/api/house/list` (保留完整路径)
  - Controller 监听：`/house/list`
  - ❌ 请求失败！

**影响范围**：所有 7 个服务的路由配置

---

## 🛠️ 修复内容

### 修复 1: Gateway 路由配置

**文件**: `apartment-gateway/src/main/resources/application.yml`

**修改**: 所有路由的 `StripPrefix=0` → `StripPrefix=1`

```yaml
# 修改前
filters:
  - StripPrefix=0

# 修改后
filters:
  - StripPrefix=1
```

**影响的路由**：
- user-service
- house-service
- order-service
- payment-service
- notification-service
- contract-service

**效果**：
- 请求：`/api/house/list`
- Gateway 去除 `/api`
- 转发到服务：`/house/list`
- ✅ 匹配 Controller 路径

---

### 修复 2: Nacos Gateway 配置

**文件**: `nacos-configs-all.yaml`

**修改**: 同步修改 Nacos 配置文件中的所有路由

```yaml
routes:
  - id: user-service
    filters:
      - StripPrefix=1  # 改为 1
  # ... 其他路由同样修改
```

---

### 修复 3: User Service Security 配置

**文件**: `apartment-user-service/src/main/java/com/example/user/config/SecurityConfig.java`

**修改**: 移除不再需要的 `/api/user/**` 路径

```java
// 修改前
.requestMatchers("/user/**", "/api/user/**", "/actuator/**").permitAll()

// 修改后
.requestMatchers("/user/**", "/actuator/**").permitAll()
```

**原因**: Gateway 现在会去除 `/api` 前缀，转发到服务的路径是 `/user/**`

---

## 📊 服务安全配置情况

| 服务 | Spring Security | 权限控制方式 | 需要修改 |
|------|----------------|------------|---------|
| User Service | ✅ 是 | SecurityFilterChain + JWT Filter | ✅ 已修复 |
| House Service | ❌ 否 | 手动检查 `X-User-Id` Header | ✅ 无需修改 |
| Order Service | ❌ 否 | 手动检查 `X-User-Id` Header | ✅ 无需修改 |
| Payment Service | ❌ 否 | 手动检查 `X-User-Id` Header | ✅ 无需修改 |
| Contract Service | ❌ 否 | 无（内部服务） | ✅ 无需修改 |
| Notice Service | ❌ 否 | 无（内部服务） | ✅ 无需修改 |

**说明**：
- 只有 User Service 使用 Spring Security
- 其他服务通过 `@RequestHeader("X-User-Id")` 手动验证用户
- 由于 Gateway 现在正确去除 `/api` 前缀，**所有服务都无需额外配置**

---

## 🚀 需要的操作

### 必须操作（Nacos 配置更新）

如果您已经在 Nacos 中导入了配置，需要更新：

1. 访问 Nacos 控制台: http://192.168.24.129:8848/nacos
2. 找到 `apartment-gateway.yaml`
3. 将所有 `StripPrefix=0` 改为 `StripPrefix=1`
4. 点击"发布"

### 需要重启的服务

- ✅ **User Service** (Security 配置已修改)
- ✅ **Gateway** (路由配置已修改)

**其他服务无需重启**

---

## ✅ 验证方法

修复后，所有接口路径应该正常工作：

```bash
# 通过网关访问
curl http://localhost:8080/api/user/login      # → /user/login
curl http://localhost:8080/api/house/list      # → /house/list
curl http://localhost:8080/api/order/my/list   # → /order/my/list
curl http://localhost:8080/api/payment/account/balance # → /payment/account/balance
```

---

## 📝 技术说明

### StripPrefix 参数含义

- `StripPrefix=0`: 不去除任何路径段
- `StripPrefix=1`: 去除第一个路径段（如 `/api`）
- `StripPrefix=2`: 去除前两个路径段（如 `/api/v1`）

### 路径转换示例

```
请求URL: http://localhost:8080/api/house/list

StripPrefix=0:
  → 转发到: http://house-service/api/house/list
  → Controller 路径: /house/list
  → ❌ 不匹配

StripPrefix=1:
  → 转发到: http://house-service/house/list
  → Controller 路径: /house/list
  → ✅ 匹配成功
```
