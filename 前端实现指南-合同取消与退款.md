# 🏢 房屋租赁平台 - "合同取消与退款"功能前端实现指南

**版本**: v1.0.0  
**作者**: 资深全栈工程师  
**更新日期**: 2026年2月7日  

---

## 📋 核心业务逻辑

### 资金守恒原则（Conservation of Money）

```
退款场景（合同取消）：
  租客钱包余额 增加 X 元  +  房东钱包余额 扣除 X 元  →  总额不变 ✅

支付场景（交租金）：
  租客钱包余额 扣除 X 元  +  房东钱包余额 增加 X 元  →  总额不变 ✅
```

**关键约束**：所有涉及两个账户的操作都必须是**原子性的**（All or Nothing）

---

## 🎯 前端架构设计

### 1. **Pinia 状态管理** (`stores/walletStore.js`)

```javascript
export const useWalletStore = defineStore('wallet', () => {
  // 核心数据
  const account = ref({
    availableBalance: 0,      // 可用余额
    frozenAmount: 0,          // 冻结金额
    totalIncome: 0,           // 累计收入
    totalExpense: 0           // 累计支出
  })
  
  const transactions = ref([]) // 交易记录
  
  /**
   * 实时刷新余额 - 支付/退款后必须调用
   * @param {string} reason - 刷新原因：'payment', 'refund', 'income'
   */
  async refreshBalance(reason = 'manual')
  
  /**
   * 完整刷新 - 同时更新余额和交易记录
   * @param {string} reason - 操作原因
   */
  async fullRefresh(reason = 'manual')
  
  /**
   * 注册余额变化监听器 - 用于跨页面更新
   */
  onBalanceChange(callback)
  
  /**
   * 资金守恒验证 - 操作前检查
   */
  validateFundsConservation(amount, type)
})
```

**关键特性**：
- ✅ 全局状态管理
- ✅ 跨页面实时同步
- ✅ 余额变化事件系统
- ✅ 资金守恒前置检查

---

### 2. **API 扩展** (`api/payment.js`)

新增接口：

```javascript
/**
 * 执行原子性退款（合同取消）
 * 后端保证原子性：要么两个账户都变更，要么都不变
 */
export function executeAtomicRefund(contractId, data) {
  return request({
    url: `/api/payment/contract/${contractId}/atomic-refund`,
    method: 'post',
    data
  })
}

/**
 * 获取退款状态
 */
export function getRefundStatus(contractId) {
  return request({
    url: `/api/payment/contract/${contractId}/refund-status`,
    method: 'get'
  })
}

/**
 * 查询资金转移历史（验证守恒）
 */
export function getMoneyTransferHistory(startTime, endTime) {
  return request({
    url: '/api/payment/transfer-history',
    method: 'get',
    params: { startTime, endTime }
  })
}
```

---

### 3. **合同详情页** (`views/contract/ContractDetail.vue`)

#### 退租申请流程：

```vue
<template>
  <!-- 退租弹窗中的资金守恒可视化 -->
  <div class="money-conservation-flow">
    <div class="flow-header">资金守恒路径 (Conservation of Money)</div>
    <div class="flow-graph">
      <div class="actor landlord">
        <span class="icon">🏠</span>
        <span class="label">房东账户</span>
        <span class="change negative">-¥{{ refundAmount }}</span>
      </div>
      <div class="arrow">
        <div class="line"></div>
        <div class="head"></div>
        <div class="amount-tip">资金返还</div>
      </div>
      <div class="actor tenant">
        <span class="icon">👤</span>
        <span class="label">租客(您)</span>
        <span class="change positive">+¥{{ refundAmount }}</span>
      </div>
    </div>
    <div class="total-conservation">
      <span>账户间变动净值：¥0.00</span>
      <span class="tag">资金守恒已验证</span>
    </div>
  </div>
</template>

<script>
import { useWalletStore } from '../../stores/walletStore'

export default {
  setup() {
    const walletStore = useWalletStore()
    
    // 提交退租申请（关键方法）
    const submitTerminate = async () => {
      // 1️⃣ 前端资金守恒验证
      const validation = walletStore.validateFundsConservation(
        parseFloat(refundAmount.value),
        'refund'
      )
      console.log('💰 资金守恒验证:', validation)
      
      // 2️⃣ 显示资金流向确认对话框
      const confirmMsg = 
        `确认退租申请？\\n` +
        `退款金额：¥${refundAmount.value}\\n` +
        `退款后，您的余额将从 ¥${walletStore.account.availableBalance.toFixed(2)} ` +
        `变为 ¥${(walletStore.account.availableBalance + parseFloat(refundAmount.value)).toFixed(2)}`
      
      if (!confirm(confirmMsg)) return
      
      // 3️⃣ 发送退租申请
      await terminateContract(contract.value.id, {
        reason: terminateForm.value.reason,
        refundAmount: refundAmount.value
      })
      
      // 4️⃣ 🔑 关键步骤：原子性完成后立即刷新钱包余额
      console.log('🔄 开始刷新钱包余额...')
      
      // 短暂延迟确保后端事务完成
      await new Promise(resolve => setTimeout(resolve, 500))
      
      // 通过 Pinia Store 刷新，会自动通知所有监听页面
      const refreshSuccess = await walletStore.fullRefresh('refund')
      
      if (refreshSuccess) {
        console.log('✅ 钱包已刷新，新余额:', walletStore.account.availableBalance)
        
        alert(
          `✅ 退租申请已成功处理！\\n\\n` +
          `退款金额：¥${refundAmount.value}\\n` +
          `✨ 您的新余额：¥${walletStore.account.availableBalance.toFixed(2)}\\n\\n` +
          `退款已入账，请在"我的钱包"页面查看详情。`
        )
      } else {
        alert(
          `退租申请已提交，但无法实时刷新余额。\\n` +
          `请手动刷新页面或访问"我的钱包"查看最新余额。`
        )
      }
      
      closeTerminateModal()
      loadContract()
    }
  }
}
</script>
```

**流程图**：
```
用户点击"确认退租"
  ↓
[验证资金守恒] ← walletStore.validateFundsConservation()
  ↓
[显示资金流向确认] ← 双方账户变化可视化
  ↓
[用户确认] → 调用后端API
  ↓
[后端原子性操作] → 两个账户同时变更或都不变
  ↓
[延迟 500ms] ← 等待后端事务提交
  ↓
[调用 fullRefresh('refund')] ← 重新加载余额和交易记录
  ↓
[触发监听事件] → 通知其他页面（如 Wallet.vue）
  ↓
[显示成功消息] ← 展示新余额
  ↓
✅ 完成
```

---

### 4. **钱包页面** (`views/payment/Wallet.vue`)

#### 实时监听余额变化：

```vue
<template>
  <!-- 实时更新提示条 -->
  <div v-if="showRefreshTip" class="refresh-notification animated-slide-down">
    <div class="notification-content">
      <span class="icon">✨</span>
      <div class="text-wrapper">
        <p class="title">
          {{refreshReason === 'refund' ? '💰 退款已到账' : 
            refreshReason === 'payment' ? '💳 支付已完成' : '🔄 钱包已更新'}}
        </p>
        <p class="detail" v-if="balanceChangeDetails?.changeAmount">
          <span v-if="balanceChangeDetails.changeAmount > 0" class="positive">
            +¥{{ Math.abs(balanceChangeDetails.changeAmount).toFixed(2) }}
          </span>
          <span v-else class="negative">
            -¥{{ Math.abs(balanceChangeDetails.changeAmount).toFixed(2) }}
          </span>
        </p>
      </div>
    </div>
  </div>

  <!-- 手动刷新按钮 -->
  <div class="refresh-bar mb-md">
    <button class="btn btn-small btn-secondary" @click="manualRefresh">
      🔄 {{ loading ? '刷新中...' : '刷新余额' }}
    </button>
  </div>
</template>

<script>
export default {
  setup() {
    const walletStore = useWalletStore()
    
    /**
     * 核心：监听钱包余额变化
     * 当其他页面（如ContractDetail）执行支付/退款时，本页面自动更新
     */
    const setupBalanceListener = () => {
      const unsubscribe = walletStore.onBalanceChange((reason, details) => {
        console.log('💳 钱包余额变化监听事件:', reason, details)
        
        // 更新页面状态，显示实时通知
        showRefreshTip.value = true
        refreshReason.value = reason
        balanceChangeDetails.value = details
        
        // 重新加载页面数据
        loadData()
        
        // 3秒后自动隐藏提示
        setTimeout(() => {
          showRefreshTip.value = false
        }, 3000)
      })
      
      return unsubscribe
    }
    
    onMounted(() => {
      loadData()
      setupBalanceListener()
      walletStore.loadWalletData()
    })
  }
}
</script>
```

**特点**：
- ✅ 不需要手动刷新，自动监听其他页面的操作
- ✅ 实时显示余额变化动画
- ✅ 3秒自动消失或手动关闭
- ✅ 支持多个操作原因区分

---

### 5. **支付页面** (`views/payment/Payment.vue`)

#### 支付后实时刷新：

```javascript
const handlePay = async () => {
  paying.value = true
  try {
    // 1️⃣ 资金守恒验证
    const validation = walletStore.validateFundsConservation(
      parseFloat(payAmount.value),
      'payment'
    )
    
    // 2️⃣ 创建支付单
    const createRes = await createPayment({...})
    
    // 3️⃣ 执行支付
    await executePayment(createRes.data.paymentNo)
    
    // 4️⃣ 🔑 支付完成后立即刷新钱包
    console.log('💳 支付已完成，开始刷新余额...')
    
    await new Promise(resolve => setTimeout(resolve, 300))
    
    // 触发全局余额更新，所有监听页面会自动刷新
    const refreshSuccess = await walletStore.fullRefresh('payment')
    
    if (refreshSuccess) {
      alert(
        `✅ 支付成功！\\n` +
        `✨ 您的新余额：¥${walletStore.account.availableBalance.toFixed(2)}`
      )
    }
    
    router.push(`/order/${order.value.orderNo}`)
  } finally {
    paying.value = false
  }
}
```

---

## 🔐 原子性操作保证

### 前端责任：

```javascript
// 1. 操作前验证
✅ 验证资金守恒条件
✅ 检查余额充足
✅ 记录操作前状态

// 2. 发起操作
✅ 调用后端API
✅ 后端执行事务（DB层面保证原子性）

// 3. 操作后确认
✅ 等待后端事务提交（500ms延迟）
✅ 调用 fullRefresh() 重新加载数据
✅ 触发监听事件通知所有页面

// 4. 失败处理
✅ 捕获异常并提示用户
✅ 不自动重试（避免重复操作）
✅ 提示用户查看钱包详情
```

### 后端配合（需要实现）：

```sql
-- 退款操作（后端需实现为单个事务）
BEGIN TRANSACTION;
  UPDATE wallet SET balance = balance - ? WHERE user_id = ? (房东)
  UPDATE wallet SET balance = balance + ? WHERE user_id = ? (租客)
  INSERT INTO transaction_log (...)
COMMIT;

-- 如果任何步骤失败，整个事务回滚
```

---

## 📊 数据流

### 支付流程数据流：

```
页面 A (Payment.vue)
  │
  ├─ walletStore.validateFundsConservation()
  │    └─ 检查余额 ✅
  │
  ├─ API: /api/payment (创建支付单)
  │    └─ 返回 paymentNo
  │
  ├─ API: /api/payment/{paymentNo}/pay (执行支付)
  │    └─ 后端：原子性更新两个账户
  │    └─ 成功返回 200
  │
  ├─ 等待 300ms (确保后端事务提交)
  │
  ├─ walletStore.fullRefresh('payment')
  │    ├─ 调用 getAccountBalance() 
  │    ├─ 调用 getAccountTransactions()
  │    ├─ 更新全局状态
  │    └─ 触发 onBalanceChange 事件
  │
  ├─ 事件传播
  │    │
  │    ├─ 页面 B (Wallet.vue) 收到事件
  │    │    └─ 自动刷新显示
  │    │
  │    ├─ 页面 C (Home.vue) 收到事件
  │    │    └─ 自动更新（可选）
  │    │
  │    └─ ...其他监听页面...
  │
  └─ 用户看到：✨ 新余额实时显示

时序：
T0:   用户点击"确认支付"
T+0ms: 验证余额
T+100ms: 创建支付单
T+500ms: 执行支付API
T+600ms: 等待后端事务
T+900ms: 刷新钱包 Store
T+950ms: 触发监听事件
T+1000ms: 所有监听页面更新
```

---

## 🎨 UI 反馈

### 1. 资金流向可视化（Wallet Flow Diagram）

```
退款示例：
┌─────────────────┐         ┌─────────────────┐
│   🏠 房东账户     │  -¥500  │   👤 租客(您)    │
│  ¥10,000.00    │←────────│  ¥2,000.00     │
│  -¥500.00      │  资金返还 │  +¥500.00      │
│  = ¥9,500.00   │         │  = ¥2,500.00  │
└─────────────────┘         └─────────────────┘

验证：9,500 + 2,500 = 12,000 ✅ 资金守恒
```

### 2. 实时通知条

```
┌─────────────────────────────────────────┐
│ ✨ 💰 退款已到账                          │ ×
│ +¥500.00 已入账                         │
└─────────────────────────────────────────┘
(3秒后自动消失)
```

### 3. 余额变动显示

```
🔄 刷新余额  [最后更新: 实时同步]

💰 可用余额
¥2,500.00

💳 冻结金额    📈 累计收入    📉 累计支出
¥0.00        ¥10,500.00    ¥8,000.00
```

---

## 🚀 安装与集成步骤

### Step 1: 安装 Pinia

```bash
npm install pinia
```

### Step 2: 在 main.js 初始化

```javascript
import { createPinia } from 'pinia'

const pinia = createPinia()
app.use(pinia)
```

### Step 3: 创建 Store 目录（如果不存在）

```bash
mkdir -p src/stores
```

### Step 4: 复制文件

```bash
# 复制 walletStore.js
cp stores/walletStore.js src/stores/

# 更新 API 
cp api/payment.js src/api/

# 更新组件
cp views/contract/ContractDetail.vue src/views/contract/
cp views/payment/Wallet.vue src/views/payment/
cp views/payment/Payment.vue src/views/payment/
```

### Step 5: 验证后端接口

确保后端实现以下接口：

```
POST   /api/payment/contract/{contractId}/atomic-refund
GET    /api/payment/contract/{contractId}/refund-status
GET    /api/payment/transfer-history
POST   /api/payment (创建支付单)
POST   /api/payment/{paymentNo}/pay (执行支付)
GET    /api/payment/account/balance (获取余额)
GET    /api/payment/account/transactions (获取交易记录)
```

---

## ✅ 测试清单

- [ ] **单个账户余额更新** 
  - [ ] 支付后余额正确减少
  - [ ] 退款后余额正确增加

- [ ] **两个账户同时更新**
  - [ ] 支付时租客-X，房东+X
  - [ ] 退款时租客+X，房东-X

- [ ] **资金守恒验证**
  - [ ] 支付前后总额不变
  - [ ] 退款前后总额不变

- [ ] **跨页面实时同步**
  - [ ] 支付页完成后，钱包页自动刷新
  - [ ] 合同页退款后，钱包页实时显示新余额

- [ ] **错误处理**
  - [ ] 余额不足时提示
  - [ ] 网络错误时提示
  - [ ] 后端异常时回滚

- [ ] **UI 反馈**
  - [ ] 加载态显示
  - [ ] 成功提示展示
  - [ ] 实时通知条消失

---

## 🐛 常见问题

### Q1: 为什么需要延迟 500ms？
**A**: 后端完成事务提交需要时间，延迟确保数据库已写入。

### Q2: 如果网络中断怎么办？
**A**: 前端会捕获错误并提示，用户可访问钱包页面手动刷新查看最新状态。

### Q3: 监听器会导致内存泄漏吗？
**A**: 不会，组件卸载时自动取消监听（`onUnmounted`）。

### Q4: 支持多个浏览器标签页同时操作吗？
**A**: 支持，但不同标签页的余额是独立的。刷新后会同步最新数据。

---

## 📚 关键代码片段速查

### 刷新余额（任何组件）
```javascript
import { useWalletStore } from '@/stores/walletStore'

const walletStore = useWalletStore()
await walletStore.fullRefresh('reason')
```

### 监听余额变化
```javascript
const unsubscribe = walletStore.onBalanceChange((reason, details) => {
  console.log('余额变化:', reason, details)
})
// 组件卸载时取消
onUnmounted(() => unsubscribe())
```

### 验证资金
```javascript
const validation = walletStore.validateFundsConservation(amount, 'payment')
if (validation.isValid) {
  // 可以进行支付
} else {
  alert(validation.message)
}
```

---

## 📞 技术支持

如有问题，请查看：
1. 浏览器 Console 日志（F12）
2. 后端 API 响应
3. Pinia DevTools 状态检查

祝您开发顺利！🚀
