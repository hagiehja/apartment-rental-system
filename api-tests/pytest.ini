[pytest]
addopts = --strict-markers
xfail_strict = true
testpaths = tests
markers =
    smoke: 健康检查和核心冒烟接口
    auth: 登录、Token 和权限接口
    business: 普通业务接口
    internal: 仅供微服务内部调用的接口
    maintenance: 数据插入、修复等维护接口
    destructive: 修改资金或关键业务状态的接口
    e2e: 完整租赁端到端流程
